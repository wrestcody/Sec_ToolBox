# Cloud Risk Prioritization Engine - Environment Configuration

# Database Configuration (choose one)
# For PostgreSQL (recommended for production)
DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/cloud_risk_db

# Alternative PostgreSQL configuration
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=cloud_risk_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password

# For SQLite (local development only)
# DATABASE_URL=sqlite:///cloud_risk_prioritization.db

# Flask Configuration
SECRET_KEY=your-secret-key-change-in-production
FLASK_DEBUG=False
HOST=0.0.0.0
PORT=5000

# Logging Configuration
LOG_LEVEL=INFO

# Security Configuration (for production)
# SSL_DISABLE=False
# PREFERRED_URL_SCHEME=https